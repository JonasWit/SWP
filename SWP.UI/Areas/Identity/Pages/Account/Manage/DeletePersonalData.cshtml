@page
@model DeletePersonalDataModel
@{
    ViewData["Title"] = "Usunięcie Danych Osobowych";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h4>@ViewData["Title"]</h4>

<div class="alert alert-warning" role="alert">
    <strong>dodac warning!!!</strong>
    <p>
        <strong>
            Usunięcie Twoich danych osobowych spowoduje także usunięcie konta!
            Usunięcie Twojego konta doprowadzi zaś do trwałego usunięcia WSZYSTKICH danych powiązanych z Twoim Profilem. Jeśli jesteś jedynym użytkownikiem który posiada dany Profil, dojdzie do nieodwracalnego usunięcia wszystkich danych zgromadzonych w aplikacjach.
        </strong>
    </p>
</div>

<div>
    <form id="delete-user" method="post" class="form-group">
        <div asp-validation-summary="All" class="text-danger"></div>
        @if (Model.RequirePassword)
        {
            <div class="form-group">
                <label asp-for="Input.Password"></label>
                <input asp-for="Input.Password" class="form-control" />
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>
        }
        <button class="btn btn-danger" type="submit">Usuń dane i zamknij moje konto!</button>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}